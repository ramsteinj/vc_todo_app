# AGENTS.md

## 목적

이 프로젝트를 저장소의 Markdown 명세에 따라 구현한다.

제품의 주요 기준 문서는 `PROJECT.md`이다.
세부 명세는 `docs/` 아래의 문서에 있다.

## 작업 전에 읽을 문서

중요한 변경을 하기 전에 다음을 읽는다.

- `PROJECT.md`
- 관련 `docs/` 문서
- 변경 대상과 관련된 기존 소스 코드

문서에 없는 요구사항을 추측하여 구현하지 않는다.

## 작업 방식

항상 작은 단위로 작업한다.

각 작업은 다음 순서를 따른다.

1. 기존 코드를 확인한다.
2. 가장 작은 유효한 구현 단위를 결정한다.
3. 코드를 수정한다.
4. 관련 테스트 또는 검증 명령을 실행한다.
5. 결과를 확인한다.
6. 문제가 있으면 수정한다.
7. 변경 내용과 검증 결과를 요약한다.

관련 없는 기능을 함께 구현하지 않는다.

## 계획 수립

여러 파일을 변경하거나 아키텍처에 영향을 주는 작업은 먼저 계획을 세운다.

계획에는 다음을 포함한다.

- 구현 방법
- 생성/수정할 파일
- 검증 방법
- 사용자의 결정이 필요한 사항

추측에 기반한 파일 수정은 하지 않는다.

## 아키텍처 규칙

- Frontend: Vue.js 3 + Vite + JavaScript + HTML5 + CSS3
- Backend: Python + Django + Django REST Framework
- Database: PostgreSQL
- Database 접근: Django ORM
- Frontend ↔ Backend 통신: REST/JSON
- Frontend에서 PostgreSQL 직접 접근 금지

요구된 Framework나 Database를 다른 기술로 교체하지 않는다.

## 코딩 원칙

- 단순하고 일반적인 구현을 우선한다.
- 성급한 추상화를 피한다.
- 불필요한 의존성을 추가하지 않는다.
- 모듈과 컴포넌트의 책임을 명확하게 한다.
- 요구사항이 변경되지 않았다면 기존 동작을 유지한다.
- MVP 범위를 임의로 확대하지 않는다.
- Framework의 일반적인 관례를 우선한다.

## 테스트 원칙

Backend 변경 후 관련 Django 테스트를 실행한다.

API 변경 후 영향을 받는 Endpoint를 검증한다.

Frontend 변경 후 관련 Frontend Check/Build를 실행하고 가능한 경우 실제 UI를 확인한다.

오류가 발생하면 실제 오류 메시지를 먼저 분석한 후 코드를 변경한다.

실제로 실행하지 않은 테스트를 통과했다고 보고하지 않는다.

## Database 원칙

스키마 변경은 Django migration으로 관리한다.

Migration 대신 PostgreSQL Schema를 수동으로 수정하지 않는다.

API Key, 비밀번호 및 기타 Secret을 코드에 직접 작성하지 않는다.

가능한 경우 환경변수를 사용한다.

## 보안 원칙

- API Key와 Password를 코드에 하드코딩하지 않는다.
- Secret이 포함된 `.env` 파일을 Git에 Commit하지 않는다.
- 사용자 입력을 검증한다.
- 내부 Stack Trace를 사용자에게 노출하지 않는다.
- 인증/권한 기능은 명시적으로 요청되지 않는 한 추가하지 않는다.

## Git 원칙

변경사항은 논리적인 단위로 유지한다.

사용자가 이미 수정한 관련 없는 변경사항을 삭제하거나 덮어쓰지 않는다.

사용자가 명시적으로 요청하지 않는 한 Commit을 생성하지 않는다.

## 작업 결과 보고

작업이 끝나면 다음을 보고한다.

- 변경한 내용
- 생성/수정한 파일
- 실행한 명령과 테스트
- 검증 결과
- 남아 있는 문제 또는 사용자의 결정이 필요한 사항

요구사항이 서로 충돌하거나 모호하면 임의로 결정하지 말고 사용자에게 질문한다.
